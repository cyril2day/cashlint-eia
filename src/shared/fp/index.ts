import { both, complement, is, isEmpty } from 'ramda'

export {
	allPass,
	anyPass,
	applySpec,
	assoc,
	assocPath,
	both,
	complement,
	compose,
	cond,
	converge,
	descend,
	dissoc,
	either,
	eqBy,
	equals,
	evolve,
	filter,
	find,
	groupBy,
	has,
	hasPath,
	ifElse,
	includes,
	identity,
	is,
	isEmpty,
	isNil,
	keys,
	map,
	mapObjIndexed,
	mergeDeepLeft,
	mergeDeepRight,
	mergeLeft,
	mergeRight,
	omit,
	path,
	pathEq,
	pathOr,
	pick,
	pipe,
	pipeWith,
	pluck,
	prop,
	propEq,
	props,
	reduce,
	reject,
	sortBy,
	toPairs,
	unless,
	values,
	when,
	where,
	whereEq,
	zipObj,
} from 'ramda'

// Small named predicate helpers to make FP migrations easier.
export const isNonEmptyString = both(is(String), complement(isEmpty))

